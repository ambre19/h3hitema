pipeline {
    agent any

    stages {
        stage('Clean mon projet') {
            steps {
                cleanWs()
            }
        }
       
        stage('CkeckOut SCM') {
            steps {
                // git branch: 'main', credentialsId: 'root', url: 'https://github.com/ambre19/h3hitema.git'
                git branch: 'main', credentialsId: 'user-ambre19', url: 'https://github.com/ambre19/h3hitema.git'
            }
        }
